# Search & Find

- grep "pattern" file — search for pattern in a file
- grep -R "pattern" . — recursive search in current directory
- find . -type f -name "*.conf" — find files by name
- locate filename — fast filename lookup (requires updatedb)