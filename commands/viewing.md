# Viewing files

- cat file — print the file to stdout
- less file — view file one page at a time (q to quit)
- head -n 10 file — show first 10 lines
- tail -n 10 file — show last 10 lines
- tail -f file — follow appended output