# Networking

- ip addr show — show interfaces and addresses
- ip link set dev eth0 up/down — bring an interface up or down
- ss -tuln — list listening sockets
- ping host — test connectivity to a host
- curl -I https://example.com — fetch response headers
- hostname — show or set the system host name