# Navigation — move around the filesystem

- ls -la — list files (including hidden) with details
- cd ~/path — change directory
- pwd — print working directory
- tree — show directory tree (if installed)